# 延迟数据记录功能使用说明

## 功能概述

在训练过程中自动记录每个用户的服务延迟数据，包括：
- 计算延迟、通信延迟、迁移延迟
- 服务状态和奖励信息
- 可见时间相关数据
- 用户和服务实例参数

## 数据文件结构

训练过程中会在实验结果目录下创建 `delay_data` 文件夹，包含以下文件：

### 1. 主要数据文件
- `user_delay_data.csv` - 详细的延迟数据记录
- `episode_XXXX_delay_summary.json` - 每5个episode的汇总统计

### 2. CSV数据字段说明

| 字段名 | 说明 | 单位 |
|--------|------|------|
| episode | Episode编号 | - |
| step | 环境步数 | - |
| total_steps | 总训练步数 | - |
| timestamp | 记录时间戳 | - |
| satellite_id | 卫星ID | - |
| user_id | 用户ID | - |
| service_status | 服务状态（True/False） | - |
| compute_delay_ms | 计算延迟 | 毫秒 |
| comm_delay_ms | 通信延迟 | 毫秒 |
| migration_delay_ms | 迁移延迟 | 毫秒 |
| total_delay_ms | 总延迟 | 毫秒 |
| delay_reward | 延迟奖励 | - |
| visibility_penalty | 可见时间惩罚 | - |
| user_reward | 用户总奖励 | - |
| remaining_visibility_time_s | 剩余可见时间 | 秒 |
| total_visibility_time_s | 总可见时间 | 秒 |
| user_task_size | 用户任务大小 | - |
| service_instance_size | 服务实例大小 | - |

## 使用方法

### 1. 训练期间自动记录

延迟数据会在训练过程中自动记录，无需额外配置。记录频率：
- 每10个训练步收集一次数据
- 每个episode结束时保存数据到CSV
- 每5个episode创建一次汇总统计

### 2. 数据分析

使用提供的分析脚本进行数据分析：

```bash
# 基本分析
python analyze_delay_data.py --data_dir ./results/your_experiment/delay_data

# 指定输出目录
python analyze_delay_data.py --data_dir ./results/your_experiment/delay_data --output_dir ./analysis_results
```

### 3. 分析结果

分析脚本会生成以下输出：

#### 图表文件
- `delay_analysis.png` - 延迟趋势综合分析图
  - 平均总延迟随Episode变化
  - 延迟组件分布
  - 服务成功率趋势
  - 用户奖励分布
  - 剩余可见时间分布
  - 延迟与可见时间关系

- `satellite_performance.png` - 各卫星性能对比图
  - 各卫星平均延迟
  - 各卫星服务次数
  - 各卫星平均用户奖励
  - 延迟标准差对比

#### 数据文件
- `satellite_statistics.csv` - 各卫星详细统计数据
- `analysis_summary.json` - 完整的分析汇总报告

### 4. 监控指标

在WandB或TensorBoard中会自动记录以下延迟相关指标：
- `delay_statistics/avg_total_delay_ms` - 平均总延迟
- `delay_statistics/min_total_delay_ms` - 最小延迟
- `delay_statistics/max_total_delay_ms` - 最大延迟
- `delay_statistics/avg_user_reward` - 平均用户奖励
- `delay_statistics/service_success_rate` - 服务成功率

## 数据分析示例

### Python代码示例

```python
import pandas as pd
import numpy as np

# 加载数据
df = pd.read_csv('delay_data/user_delay_data.csv')

# 分析服务成功率趋势
success_rate_by_episode = df.groupby('episode')['service_status'].mean()
print("服务成功率趋势:")
print(success_rate_by_episode.head(10))

# 分析延迟组件
successful_services = df[df['service_status'] == True]
delay_components = successful_services[['compute_delay_ms', 'comm_delay_ms', 'migration_delay_ms']].mean()
print("\n平均延迟组件:")
print(delay_components)

# 分析奖励与延迟关系
correlation = successful_services['total_delay_ms'].corr(successful_services['user_reward'])
print(f"\n延迟与奖励的相关性: {correlation:.3f}")
```

### SQL查询示例

```sql
-- 查询各卫星的平均性能
SELECT 
    satellite_id,
    COUNT(*) as service_count,
    AVG(CASE WHEN service_status = 1 THEN total_delay_ms END) as avg_delay,
    AVG(user_reward) as avg_reward,
    SUM(CASE WHEN service_status = 1 THEN 1 ELSE 0 END) * 1.0 / COUNT(*) as success_rate
FROM user_delay_data 
GROUP BY satellite_id
ORDER BY avg_reward DESC;

-- 查询延迟改善趋势
SELECT 
    episode,
    AVG(CASE WHEN service_status = 1 THEN total_delay_ms END) as avg_delay,
    COUNT(CASE WHEN service_status = 1 THEN 1 END) as successful_services
FROM user_delay_data 
GROUP BY episode
ORDER BY episode;
```

## 注意事项

1. **存储空间** - 延迟数据会积累较大的文件，建议定期清理或压缩旧数据
2. **性能影响** - 数据收集频率可以通过修改代码中的采样间隔来调整
3. **数据完整性** - 确保训练过程不中断，避免数据丢失
4. **分析工具** - 建议使用pandas、matplotlib等工具进行自定义分析

## 扩展功能

### 自定义分析

可以根据需要编写自定义分析脚本：

```python
def analyze_migration_effectiveness(df):
    """分析迁移效果"""
    migration_services = df[df['migration_delay_ms'] > 0]
    no_migration_services = df[df['migration_delay_ms'] == 0]
    
    migration_avg_reward = migration_services['user_reward'].mean()
    no_migration_avg_reward = no_migration_services['user_reward'].mean()
    
    print(f"迁移服务平均奖励: {migration_avg_reward:.3f}")
    print(f"无迁移服务平均奖励: {no_migration_avg_reward:.3f}")
    print(f"迁移效果提升: {migration_avg_reward - no_migration_avg_reward:.3f}")

def analyze_visibility_impact(df):
    """分析可见时间对性能的影响"""
    successful_df = df[df['service_status'] == True]
    
    # 按可见时间分段分析
    bins = [0, 20, 40, 60, 100, float('inf')]
    labels = ['0-20s', '20-40s', '40-60s', '60-100s', '100s+']
    successful_df['visibility_range'] = pd.cut(
        successful_df['remaining_visibility_time_s'], 
        bins=bins, 
        labels=labels
    )
    
    visibility_analysis = successful_df.groupby('visibility_range').agg({
        'total_delay_ms': 'mean',
        'user_reward': 'mean',
        'user_id': 'count'
    })
    
    print("可见时间对性能影响:")
    print(visibility_analysis)
```

## 故障排除

### 常见问题

1. **CSV文件为空** - 检查训练是否正常运行，确认有用户服务数据
2. **分析脚本报错** - 确认安装了所需的Python包（pandas, matplotlib, seaborn）
3. **内存不足** - 对于大量数据，可以分批处理或增加采样间隔

### 日志查看

查看训练日志中的延迟数据记录信息：
```
grep "延迟数据" training.log
grep "已保存.*条延迟数据记录" training.log
``` 